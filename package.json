{
  "name": "yt-music-analyzer",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "cd backend && uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:web": "wait-on -t 30000 tcp:127.0.0.1:8000 && cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && uv run uvicorn app.main:app --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.1.0"
  }
}
